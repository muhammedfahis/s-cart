apiVersion: apps/v1
kind: Deployment
metadata:
  name: products-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: products-mongo
  template:
    metadata:
      labels:
        app: products-mongo
    spec:
      containers:
      - name: products-mongo
        image: mongo:4.2.13
        ports:
        - containerPort: 27017
---


apiVersion: v1
kind: Service
metadata:
  name: products-mongo-svc
spec:
  selector:
    app: products-mongo
  ports:
    - protocol: TCP
      port: 27017
      targetPort: 27017
      name: products-mongo